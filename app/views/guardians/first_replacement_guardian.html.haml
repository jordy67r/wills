= render 'header'
%h4 First Replacement Guardian


= form_for [@will, @guardian] do |f|

  .field
    = render "layouts/radio_buttons", :f => f, radio_button: :replacement_guardian, q_no: "05060", option: {true: "Yes", false: "No"}, selected: -1
  %div{"data-condition" => ":checked", "data-require" => "#guardian_replacement_guardian_true", "data-true" => "hide"}
    = f.submit 'Save', class: "button radius"

%div{"data-condition" => ":checked", "data-require" => "#guardian_replacement_guardian_true", "data-false" => "hide"}
  = form_for [@will, @guardian] do |f|
    = f.fields_for :first_replacement_guardian_general_detail do |builder|
      = render "general_details/general_details_form", :builder => builder, :nested_origin => @guardian.first_replacement_guardian_general_detail
    .field
      = question "05070"
      = f.check_box :replacement_second_guardian

    = f.submit 'Save', class: "button radius"