- provide(:title, @user.name)
%h1= @user.name

%p
  %b Name:
  = @user.name
%p
  %b Email:
  = @user.email
%p
  %b Password digest:
  = @user.password_digest
%p
  %b Remember token:
  = @user.remember_token
%p
  %b Admin:
  = @user.admin

= link_to 'Edit', edit_user_path(@user)
\|
= link_to 'Back', users_path
